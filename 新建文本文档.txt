 
//1. toucheend 还要设定其他的状态下的行为，主要changing 
//2. 优化获取，不要rankstatus了 
//3. 目前再changing的时候不会判断hoverapp，这一点需要改变下，让hoverapp，最好设定一个flag，在changing的动话时也可以改变transform

4. 判断hover时的遍历需要优化, 最好根据图标的平面位置来设置遍历顺序,先遍历身边的元素,再一圈圈往外。（目前已经debounce)
这个算法。最好改成根据坐标来判断最近的元素。知道元素left和top以及icon的布局，可以算出最近的元素。

5. 一些遍历修改样式的代码查看是否可以优化
6. 属性应用和判断,目前transform没有判断兼容性
7.  事件绑定应用代理模式
8. 操作dom需要优化一下，最好脱离文档流，减少主线程和webkit的交互
9. 

bug
1. changing时，在某个时间点松开或者移动鼠标，会让的bounce失效，导致多个resetchangeview会触发。这一点通过调搞的bounce的delay可以解决。（过一段时间后又不需要了，不知道是否和
2. 给这个项目画张业务逻辑流程图才行，不然自己都看不懂了。